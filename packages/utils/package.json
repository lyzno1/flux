{
	"devDependencies": {
		"@flux/config": "workspace:*",
		"typescript": "catalog:dev",
		"vitest": "catalog:dev"
	},
	"exports": {
		"./*": {
			"default": "./src/*.ts"
		}
	},
	"name": "@flux/utils",
	"scripts": {
		"check-types": "tsc --noEmit",
		"test": "vitest run",
		"test:coverage": "vitest run --coverage"
	},
	"type": "module"
}
