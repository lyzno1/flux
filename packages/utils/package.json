{
	"devDependencies": {
		"@flux/config": "workspace:*",
		"jsdom": "catalog:dev",
		"typescript": "catalog:dev",
		"vitest": "catalog:dev"
	},
	"exports": {
		"./*": {
			"default": "./src/*.ts"
		}
	},
	"name": "@flux/utils",
	"scripts": {
		"check-types": "tsgo --noEmit",
		"test": "vitest run",
		"test:coverage": "vitest run --coverage"
	},
	"type": "module"
}
